/*
** EPITECH PROJECT, 2019
** enum.h
** File description:
** enum.h
*/

#ifndef ENUM_H_
#define ENUM_H_

enum bool {
    TRUE = 0,
    FALSE
};

enum button_t {
    STAR = 6,
    SETG,
    HOME,
    RESTAR,
    RESUME
};

enum exit_t {
    EXIT = 84
};

enum is_game {
    MENU = 2,
    PAUSE,
    GAME,
    INTRO
};

enum button {
    PUSHED = 11,
    NOT_PUSHED
};

#endif
